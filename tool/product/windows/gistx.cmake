# Copied to windows/product.cmake by tool/build.dart before every Windows
# build (see _applyProductConfigs). Do not hand-edit windows/product.cmake
# directly -- edit this template instead.
#
# windows/product.cmake's own committed content is always this file's
# content, so a fresh checkout or a crashed build leaves the tree buildable
# without running the tool first.
set(BINARY_NAME "gistx")
